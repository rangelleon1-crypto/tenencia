{
  "name": "consulta-vehicular-api",
  "version": "1.0.0",
  "description": "API para consulta de estado de cuenta vehicular con proxy",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "prestart": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "playwright": "^1.40.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
